# 2026-08-23 작업 기록 — 토스페이먼츠 결제 결함 수정 및 문구 정리

> 대상: `La-Ligne-Homme-frontend`
> 백엔드(`La-Ligne-Homme-Backend`)에도 짝을 이루는 변경이 함께 올라갔습니다.
> 결함의 상세 원인과 서버 측 수정 내용은 백엔드 저장소의 `logs/20260823.md` 를 참고하세요.

---

## 1. 배경

토스페이먼츠 연동을 진행하기 앞서 기존 결제 코드를 점검한 결과,
**실제 돈이 잘못 처리될 수 있는 결함 3건**이 확인되어 먼저 수정했습니다.

---

## 2. 결제 관련 수정

### ① 결제 승인을 한 번의 요청으로 통합 — `PaymentSuccess.tsx`

**이전**
결제창을 통과한 뒤 이 화면이 승인(`/toss/confirm`)과 주문 생성(`/api/orders`)을 **각각 따로** 호출했습니다.
승인은 성공했는데 주문 생성이 실패하면 **결제 기록만 남고 주문이 사라졌습니다.**

**이후**
서버에 한 번만 요청하고, 서버가 승인과 주문 저장을 한 묶음으로 처리합니다.
- 새로고침·개발 모드 이중 실행으로 승인이 두 번 나가지 않도록 요청 가드 추가
- 결제 정보가 없거나 주문 정보를 찾지 못하면 명확한 안내 화면 표시

### ② 결제창 클라이언트 키를 환경 변수로 분리 — `Checkout.tsx`

**이전** — 소스 코드에 테스트 키가 박혀 있었습니다.
```ts
const clientKey = "test_gck_docs_Ovk5rk1EwkEbP0W43n07xlzm";
```

**이후** — `.env` 의 `VITE_TOSS_CLIENT_KEY` 로 관리합니다.
- 키가 **비어 있으면** 결제 화면에 '온라인 결제' 선택지가 나타나지 않고 무통장입금만 보입니다.
- 키를 **채우면** 코드 수정 없이 온라인 결제가 자동으로 살아납니다.
- 테스트 → 운영 전환은 이 값만 `live_gck_...` 로 바꾸면 됩니다.

> 기존에는 이 선택지가 주석 처리되어 있어, 되살리려면 코드를 직접 고쳐야 했습니다.

### ③ 결제창 중복 렌더링 수정 — `Checkout.tsx`

**이전** — 결제 금액이 바뀔 때마다 `useEffect` 가 다시 실행되어
결제창이 화면에 여러 개 겹쳐 그려지고 SDK를 매번 새로 내려받았습니다.

**이후** — 결제창은 한 번만 그리고, 금액 변경은 `updateAmount()` 로 갱신만 합니다.

### ④ 주문 번호 생성 방식 개선 — `Checkout.tsx`

토스 원장과 우리 원장을 하나의 번호로 대조할 수 있도록, 결제창에 넘긴 번호를 그대로 주문 번호로 사용합니다.
`Math.random()` 기반이던 번호를 `crypto.randomUUID()` 기반으로 바꿔 충돌 가능성을 낮췄습니다.

### ⑤ 환불 처리를 서버로 이관 — `AdminMain.tsx`

**이전**
```ts
if (targetOrder && targetOrder.paymentKey) { /* 환불 호출 */ }
```
목록 데이터에 `paymentKey` 가 들어 있지 않아 이 조건은 **항상 거짓**이었습니다.
관리자가 '주문취소'를 눌러도 상태만 바뀌고 **실제 카드 환불은 한 번도 나가지 않았습니다.**

**이후**
상태 변경 요청 하나만 보내면 서버가 결제 원장을 직접 찾아 환불과 재고 복구까지 처리합니다.
환불이 실패하면 서버가 상태 변경 자체를 되돌리고, 화면은 그 사유를 그대로 보여줍니다.

### ⑥ 개인정보 콘솔 출력 제거 — `Checkout.tsx`

로그인 세션 전체와 주문 정보를 `console.log` 로 찍던 코드를 삭제했습니다.

---

## 3. 사용자에게 노출되던 개발자 용어 정리

### 영문·한글이 섞여 있던 메인 페이지 문구

일괄 치환 사고로 보이는 흔적이 홈 화면에 그대로 노출되고 있었습니다.

| 이전 | 이후 |
|---|---|
| `깔끔한 실루엣 and 미니멀한 디자인으로...` | 깔끔한 실루엣**과** 미니멀한 디자인으로... |
| `깔끔한 실루엣 and 미니멀한 디자인이 특징이며...` | 깔끔한 실루엣**과** 미니멀한 디자인이 특징이며... |
| `brand가 지키는 것들` | **브랜드**가 지키는 것들 |
| `남성 컨템포러리 패션 brand` | 남성 컨템포러리 패션 **브랜드** |
| 푸터 제목 `brand` | **브랜드** |

### 안내창·화면 문구

| 파일 | 이전 | 이후 |
|---|---|---|
| Checkout | `백엔드 정문 코어 결제 모듈 통신 실패` | 주문을 접수하지 못했습니다. 잠시 후 다시 시도해 주세요. |
| Checkout | `결제 대상 상품이 유실되었습니다. 장바구니로 복귀합니다.` | 주문할 상품이 없습니다. 장바구니로 이동합니다. |
| Checkout | `안전한 명품 배송을 위해 배송지 명세를 성실히 기입해 주십시오.` | 배송지 정보를 모두 입력해 주세요. |
| Checkout | `우편번호 서비스 스크립트를 로딩 중입니다.` | 주소 검색을 준비하고 있습니다. 잠시 후 다시 눌러 주세요. |
| Checkout | `창고 재고 수량 초과` | 주문을 접수하지 못했습니다. 재고 수량을 확인해 주세요. |
| Login | `백엔드 서버 연동 상태를 체크하십시오.` | 로그인 중 문제가 발생했습니다. 잠시 후 다시 시도해 주세요. |
| Signup | `라  린느 인증 백엔드 서버와 통신할 수 없습니다.` (오타 포함) | 회원가입을 완료하지 못했습니다. 잠시 후 다시 시도해 주세요. |
| Signup | `인증번호가 발송되었습니다. (테스트 모드 시 백엔드 콘솔창 확인)` | 인증번호를 발송했습니다. 문자를 확인해 주세요. |
| Signup | `인증 서버 통신 실패. 백엔드 가동 상태를 확인하세요.` | 인증번호를 발송하지 못했습니다. 잠시 후 다시 시도해 주세요. |
| 비밀번호찾기 | `임시 비밀번호가 카카오 알림톡(pw_find_01)으로 발송되었습니다.` | 임시 비밀번호를 카카오 알림톡으로 발송했습니다. |
| 비밀번호찾기 | `인증 코어 서버 통신 실패. 백엔드 가동 상태를 확인하세요.` | 임시 비밀번호를 발급하지 못했습니다. 잠시 후 다시 시도해 주세요. |
| 비회원조회 | `백엔드 서버 연동 레이어 통신 실패` | 주문 내역을 불러오지 못했습니다. 잠시 후 다시 시도해 주세요. |
| 비회원조회 | `패스워드 장벽을 넘지 못했습니다.` | 주문 번호 또는 비밀번호가 일치하지 않습니다. |
| 비회원조회 | `실시간 배송 흐름 교차 검증 센터입니다.` | 주문 번호와 비밀번호로 주문 내역과 배송 상태를 확인하실 수 있습니다. |
| 비회원조회 | `교차 검증 중...` / `INVOICE SPEC (주문 영수증 명세)` | 조회 중... / ORDER DETAILS (주문 상세 내역) |
| 비회원조회 | `최종 정산 금액` / `실시간 배송 동향` | 결제 금액 / 배송 상태 |
| 마이페이지 | `실시간 주문 데이터 라우팅 중...` | 주문 내역을 불러오는 중입니다... |
| 마이페이지 | `비밀번호 변경 코어 서버와의 통신에 실패했습니다.` | 비밀번호를 변경하지 못했습니다. 잠시 후 다시 시도해 주세요. |
| 마이페이지 | `보안 데이터 암호화 중...` | 변경 중... |
| 상품상세 | `존재하지 않거나 보관함 처리된 상품입니다.` | 현재 판매하지 않는 상품입니다. |
| 상품상세 | `₩0 (라 린느 마스터 회원 전품목 전격 무료배송 혜택선 가동)` | ₩0 (전 품목 무료배송) |
| 관리자 | `스프링 부트 백엔드 본체 및 DB로 상품 등록 완료!` | 상품을 등록했습니다. |
| 관리자 | `서ver 명세 에러` (한영 혼용 오타) | (서버 메시지 그대로 표시) |
| 관리자 | `운송장 정보가 데이터베이스 원장에 안전하게 영속화되었습니다.` | 운송장 정보를 저장했습니다. |
| 관리자 | `보관함 처리(Soft Delete) 됩니다.` | 판매 목록에서만 내려가며, 기존 주문 내역은 그대로 보존됩니다. |
| 관리자 | `백엔드 회원 제어 API 서버가 꺼져있습니다.` | 회원 권한을 변경하지 못했습니다. 잠시 후 다시 시도해 주세요. |
| 관리자 | `수집된 주문 상세 상품 내역 패킷이 존재하지 않습니다.` | 주문 상품 내역이 없습니다. |
| 관리자 | `라 린느 옴므 브랜드 system 제어 콘솔입니다.` | 라 린느 옴므 브랜드 관리자 페이지입니다. |
| 관리자 | `실시간 동적 카테고리 제어 보드` / `+ 실시간 개통` | 카테고리 관리 / + 카테고리 추가 |

---

## 4. 검증

- `npm run build` **성공** (타입 체크 포함)
- 백엔드와 함께 로컬 실DB 기준 전 구간 동작 확인
  - 금액 위변조 차단 / 재고 이중 차감 해소 / 취소 시 재고 복구·결제 취소

---

## 5. 환경 설정

`.env.example` 을 추가했습니다. 복사해서 `.env` 로 만들어 사용하세요.

```bash
VITE_API_URL=http://localhost:8080
VITE_TOSS_CLIENT_KEY=      # 상점 키를 받으면 여기에 입력
```

`VITE_TOSS_CLIENT_KEY` 는 브라우저에 노출되어도 되는 **공개 키**입니다.
시크릿 키는 절대 이 파일에 넣지 마세요. (서버의 `TOSS_SECRET_KEY` 로 관리)

---

## 6. 다음 작업

1. 상점 키 입력 → 온라인 결제 자동 활성화 (코드 수정 불필요)
2. 테스트 결제 후 카드사 심사용 결제경로 자료(PPT) 제작
3. 운영 전환 시 `live_gck_...` 로 교체
4. (검토 필요) `@tosspayments/payment-widget-sdk@0.12` 는 구버전 — v2 마이그레이션 대상
5. (검토 필요) 카드사 심사 대비 화면 보완
   - 푸터 사업장 주소에 시/도 누락 (`중구 광복로49번길 33` → `부산광역시 중구 …`)
   - 상품 상세에 교환·반품 규정 블록 추가
   - 로그인 화면에 '비회원 구매' 버튼 노출
